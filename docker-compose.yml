version: '3.8'

services:
  blacktopbrews:
    build: .
    container_name: blacktopbrews
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
    environment:
      - PORT=3000
      - NODE_ENV=production
      - ADMIN_PASSWORD=${ADMIN_PASSWORD:?ADMIN_PASSWORD must be set in environment or .env file}
      - SESSION_SECRET=${SESSION_SECRET:?SESSION_SECRET must be set in environment or .env file}
    restart: always
